# CONTEXT.md — Monad Blitz 领域术语表

> 面向领域专家的术语定义，随拷问会话实时更新。

## 术语

| 术语 | 定义 | 状态 |
|------|------|------|
| **MOD** | Monad 链的原生 gas token。本产品中的资产口径待确认（仅原生 MOD vs 含 ERC-20）。 | 部分确认 |
| **押金托管（Escrow）** | 参与者在注册时将押金（默认 10 MOD，参数化）存入抽奖合约并锁至开奖，开奖后退回。押金即参与资格的唯一凭证。 | ✅ 已确认 |
| **注册交易（Register）** | 参与者从自己的钱包发起 `register()` 交易并附带 10 MOD 押金。一笔交易同时完成：地址所有权证明 + 押金托管 + 链上资格名单。 | ✅ 已确认 |
| **注册模式（Registration Mode）** | 同一合约的两种部署形态，由构造函数 issuer 参数决定：零地址 = 线上无许可模式（任何人可注册）；非零地址 = 线下活动模式（注册须附签发者准入签名）。 | ✅ 已确认 |
| **资格（Eligibility）** | 完成注册交易且押金处于锁仓状态的地址。一地址 = 一份押金 = 一次资格（防女巫）。 | ✅ 已确认 |
| **锁仓期（Lock Period）** | 押金自注册冻结至开奖公布（Drawn 状态）的机制：合约不设独立的 lockPeriod 参数，锁仓终点 = 开奖；`drawTime`（= 注册截止 = 最早开奖时间，正式默认 3 天，演示分钟级）决定锁仓时长上限，晚注册者锁仓相应缩短。锁仓期内合约不提供任何取回函数。 | ✅ 已确认 |
| **抽奖（Lottery）** | 从满足资格的地址集合中公平选出 N 名获奖者的过程。N = winnerCount（创建活动时配置，默认 1）；实际中奖人数 = min(N, 参与人数)。单期设计，多期列入未来规划。 | ✅ 已确认 |
| **均分派奖（Equal Split）** | 多名中奖者的派奖规则：每名中奖者获得 开奖时刻奖池快照 ÷ 实际中奖人数。快照在 VRF 回调时刻定格；Drawn 后奖池不再接受追加注入（防资金滞留）。wei 级尾差滞留合约，可忽略。 | ✅ 已确认 |
| **创建活动（Activity Creation）** | 主办方通过前端 /create 页连接钱包、在浏览器内直接部署抽奖合约的自助流程：可配置准入模式（issuer）、押金额、开奖时间、中奖人数，无需命令行。部署成功即得到带 `?addr=` 的活动链接。 | ✅ 已确认 |
| **领回（Claim Deposit）** | 开奖后参与者通过 pull 模式自行发起交易领回 10 MOD 押金。锁仓期内合约不提供任何取回函数。 | ✅ 已确认 |
| **Pyth Entropy** | Monad testnet 上的 VRF 服务（V2 接口）。入口为 ERC-1967 代理 `0x825C0390f379C631F3CF11a82a37d20bddf93C07`，默认 provider `0x6CC14824Ea2918f5De5C2f75A9Da968ad4BD6344`，请求费实测 0.128 MON（动态）。请求方附带 32 字节用户种子，与 Pyth 验证者熵混合产生可验证随机数；回调为 `entropyCallback(uint64,address,bytes32)`。定时开奖与现场开奖共用此随机源。产品文档旧地址 `0x3682...e320` 已证伪（仅治理函数）。 | ✅ 已确认 |
| **用户种子（User Seed）** | 发起 Entropy 请求时附带的自定义熵：定时开奖由项目方提供，现场开奖由现场观众集体产生（扫码/报数汇总）。 | ✅ 已确认 |
| **奖池（Prize Pool）** | 由赞助方/主办方单独注入合约的资金，用于向中奖者派奖。与押金严格分离：押金开奖后 100% 退回，不参与派奖。 | ✅ 已确认 |
| **定时开奖** | 到达预设时间后由触发者执行的开奖。开奖条件：到期 + 资格名单非空，满足后任何人可发起交易触发（Permissionless），合约通过 Pyth Entropy 取随机数。 | ✅ 已确认 |
| **现场开奖** | 在活动现场引入外部熵完成的开奖：观众扫码打开页面，各自产生随机数，前端将所有熵 hash 合并作为 userSeed 发起开奖交易。 | ✅ 已确认 |
| **集熵（Entropy Gathering）** | 现场观众通过扫码参与页面共同产生随机熵的交互过程，是"把现场变成随机数源"的产品亮点。聚合通道为**链上提交**：观众各自发交易 `submitEntropy(bytes32)`，合约记录事件，任何人可按提交顺序 keccak256 聚合全部熵得到 userSeed。 | ✅ 已确认 |
| **熵根（Entropy Root）** | 合约内维护的集熵累积哈希：每收到一次熵提交即 `entropyRoot = keccak256(abi.encodePacked(entropyRoot, e))`。熵根非零时 draw 强制 `userSeed == entropyRoot`（现场模式语义），为零时接受任意 userSeed（定时模式公开承诺语义）。见 ADR 0003。 | ✅ 已确认 |
| **签发者（Issuer）** | 线下活动模式下，由主办方工作人员控制的签名密钥。在签到处核验物理在场（门票/胸牌）后，对参与者地址签发准入签名。签发者密钥无提款权限，仅能授权注册。 | ✅ 已确认 |
| **准入签名（Admission Signature）** | Issuer 对参与者地址的 ECDSA 签名（含活动 ID 与有效期）。合约 register() 校验签名有效才接受押金，绑定"物理在场"与"链上地址"。 | ✅ 已确认 |

## 关键约束

- **当天交付**：12:00 定稿方案，13:00 开发，17:00 录屏，18:00 提交（见 docs/schedule.md）。所有设计必须可在 ~4 小时内开发完成，且演示不能依赖等待真实 3 天。
- **信任是产品核心卖点**：任何引入链下信任假设的方案（私有 Keeper、中心化快照）都与产品定位冲突，需特别论证。
